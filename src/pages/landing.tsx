import DefaultLayout from "../layouts/defaultLayout";
const Landing = () => {
  return (
    <DefaultLayout>
      <div className="h-screen w-full bg-img-venice bg-center bg-cover bg-fixed"></div>
    </DefaultLayout>
  );
};

export default Landing;
